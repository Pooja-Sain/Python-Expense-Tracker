# Core app
fastapi==0.104.1
starlette==0.27.0
uvicorn==0.24.0
SQLAlchemy==2.0.52
pydantic==2.12.5
pydantic_core==2.41.5
pandas==3.0.5
numpy==2.5.2

# Auth / sessions -- these two are easy to forget because nothing in the
# code imports them directly by these names (SessionMiddleware and bcrypt
# pull them in internally), so a plain "pip install fastapi uvicorn" alone
# will start the server but then crash with ModuleNotFoundError the moment
# a request needs a session or a password check.
itsdangerous
bcrypt

# File upload support (CSV import) and the test suite
python-multipart==0.0.6
pytest==9.1.1

# Transitive dependencies of the above, pinned for a reproducible install
annotated-types==0.7.0
anyio==3.7.1
click==8.3.1
colorama==0.4.6
h11==0.16.0
idna==3.11
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
python-dateutil==2.9.0.post0
six==1.17.0
sniffio==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.3
